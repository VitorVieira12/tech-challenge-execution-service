apiVersion: v1
kind: Service
metadata:
  name: execution-service
  namespace: execution-service
spec:
  selector:
    app: execution-service
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
  type: ClusterIP
